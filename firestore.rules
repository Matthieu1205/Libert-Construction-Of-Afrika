rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Lecture publique (site vitrine)
    match /lca/{document} {
      allow read: if true;
      // Écriture uniquement si authentifié (backoffice)
      allow write: if request.auth != null;
    }
  }
}
